#!/usr/bin/env python3
import base64, zlib
from pathlib import Path
# Placeholder - user runs deploy which needs real B64; fetch from artifacts via curl a88d + inline patch
print('Use tools/deploy_ui.sh after git pull')
